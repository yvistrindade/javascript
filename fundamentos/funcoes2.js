/**
 * estudo das funções simples: com parametros e retorno
 * @author yvis trindade
 */

somar(5, 2)
function somar(a, b) {
return (console.log(a + b))
}

somarAF(5, 2)
const somarAF = function (a, b) {
 return (console.log(a + b))
}

somarAFS(2, 2)
const somarAFS = (a,b) =>  {
return (console.log(a + b))
}